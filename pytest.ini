[pytest]
DJANGO_SETTINGS_MODULE = core.settings

python_files = tests.py test_*.py *_tests.py

addopts =
    --cov=.
    --cov-report=term
    --cov-report=json
    --cov-report=html
    --cov-fail-under=80

